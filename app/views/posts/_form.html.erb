
<%= form_for(@post) do |f| %>
  <%= f.text_area :content, class: "form-control", placeholder: "Enter your text", rows: 3  %>
  <%= f.submit "New Post", style: "margin-top: 10px;", class: "btn btn-primary pull-right" %>
<% end %>
